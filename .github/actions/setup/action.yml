name: Setup
description: 'Setup stuff'
runs:
  using: "composite"
  steps:
    - name: set up JDK 25
      uses: actions/setup-java@v5.2.0
      with:
        distribution: 'zulu'
        java-version: '25'
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v6
