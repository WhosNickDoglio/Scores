name: Setup
description: 'Setup stuff'
runs:
  using: "composite"
  steps:
    - name: set up JDK 26
      uses: actions/setup-java@v5.6.0
      with:
        distribution: 'zulu'
        java-version: '26'
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v6
