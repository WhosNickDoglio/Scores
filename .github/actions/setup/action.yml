name: Setup
description: 'Setup stuff'
runs:
  using: "composite"
  steps:
    - name: set up JDK 20
      uses: actions/setup-java@v3.13.0
      with:
        distribution: 'zulu'
        java-version: '20'
    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2.9.0
